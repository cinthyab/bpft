<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8"/>
	<title>Bon Profit</title>
	<link href='http://fonts.googleapis.com/css?family=Droid+Sans:400,700|Cookie|Satisfy' rel='stylesheet' type='text/css'>
  	<%= stylesheet_link_tag    "application", :media => "all" %>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>
</head>
<body style="background: #B8D8D7;">
<div class="headerbox">

	<div class="logo">
		<%= link_to (image_tag ("/system/receta/fotos/logo.svg")), welcome_index_path %>
		<!--(image_tag ("/system/receta/fotos/logo.svg"))-->
	</div>
	
	<div id="user_nav">
		  <% if user_signed_in? %>
		    <div class="correousuario">
			    <%= current_user.email %>
			  	<div class="cerrarsesion">
			  		<%= link_to (image_tag ("/system/receta/fotos/cerrar.svg")), destroy_user_session_path, :method => :delete %>
			  	</div>
		    </div>
		  <% else %>
		  <div class="correousuario">
		    <%= link_to "regístrate", new_user_registration_path %> o <%= link_to "inicia sesión", new_user_session_path %>
		  </div>
		  <% end %>
	</div>


	<% flash.each do |name, msg| %>
		<%= content_tag :div, msg, :id => "flash_#{name}" %>
	<% end %>
	
<%= yield %>

</div>

</body>
</html>
